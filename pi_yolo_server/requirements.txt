# pi_yolo_server runtime deps (Raspberry Pi 5).
# NOTE: HailoRT (the `hailo_platform` Python module) is NOT pip-installed here —
# install it on the Pi via Hailo's packages, e.g. `sudo apt install hailo-all`
# (Raspberry Pi OS, AI HAT+) which provides the PCIe driver + HailoRT.
flask>=3.0
numpy>=1.24
opencv-python-headless>=4.8
