tof_serial_driver:
  ros__parameters:
    port: "/dev/ttyACM0"
    baud: 115200
    frame_id: "tof_link"
    field_of_view: 0.44
    min_range: 0.03
    max_range: 2.0
    pattern: "Range is\\s+(\\d+)\\s*mm"
